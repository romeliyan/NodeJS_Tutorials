//client sends a request to the server and
//server sends the response to it to the client

//these two communicate through protocols

//client-------socket--------server

//protocols FTP - file transfer  HTTP - web sites  (structure of the information is decided [HTTP / FTP])

//information is sent down the socket between two computers(server and client) via TCP

//NodeJS gives us the ability to access this functionality to open a connection between two computers and send information between them

//ports
//a program running on a computer can listen for a requests sent to a particular port number
//eg: 172.24.86.76: 30000 - port 3000
